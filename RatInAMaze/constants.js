export const MATRIX_SIZE = 5;

export let DELAY_TIME = 0;

export const setDelay = (time) => {
  DELAY_TIME = time || 0;
};

export const COMMON_STYLES = {
  backgroundSize: "cover",
  backgroundRepeat: "no-repeat",
  backgroundSize: "40px 40px",
  backgroundPosition: "center",
};
